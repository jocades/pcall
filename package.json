{
  "name": "rpc",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch packages/pcall/test/server.test.ts",
    "dev:next": "bun --cwd packages/pcall/test/adapters/next dev",
    "sub": "bun --cwd apps/sub dev",
    "check": "tsc",
    "build": "scripts/build pcall",
    "publish": "bun scripts/publish.ts pcall"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
