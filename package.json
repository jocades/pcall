{
  "name": "rpc",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun --watch packages/rpc/test/server.test.ts",
    "dev:next": "bun --cwd packages/rpc/test/adapters/next dev",
    "check": "tsc",
    "build": "scripts/build rpc",
    "publish": "bun scripts/publish.ts rpc"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
