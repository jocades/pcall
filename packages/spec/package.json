{
  "name": "spec",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch index.ts",
    "start": "NODE_ENV=production bun run index.ts",
    "build": "bun run build.ts && tsc --project tsconfig.d.json",
    "build:min": "NODE_ENV=production bun run build.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "rpc": "workspace:*",
    "zod": "^3.23.8",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
